/**
 * @module contextCollectors/notion
 * Back-compat shim — real implementation lives in ./notion/index.ts
 * Prefer importing from './notion/index' or the directory itself where supported.
 */
export { collectNotion } from './notion/index';
